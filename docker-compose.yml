version: '3.8'

services:
  db:
    image: postgres:15-alpine
    container_name: salemanager-db
    restart: always
    environment:
      POSTGRES_DB: SaleManagerDB
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  backend:
    build: 
      context: ./backend
      dockerfile: Dockerfile
    container_name: salemanager-backend
    restart: always
    ports:
      - "8080:8080"
    environment:
      - ConnectionStrings__DefaultConnection=Host=db;Database=SaleManagerDB;Username=postgres;Password=postgres
      - ASPNETCORE_ENVIRONMENT=Production
    depends_on:
      - db

  frontend:
    build: 
      context: ./frontend
      dockerfile: Dockerfile
    container_name: salemanager-frontend
    restart: always
    ports:
      - "3000:3000"
    environment:
      - API_PROXY_TARGET=http://backend:8080/api/**
    depends_on:
      - backend

volumes:
  postgres_data:
